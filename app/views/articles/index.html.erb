<h1>Articles</h1>

<ul>

    <% @articles.each do |article| %>
    <li> id: <%= article.id %> - Title: <%= article.title %>  </li>
    <%= button_to 'delete', article_path(article), method: :delete %>

    <% end %>
<br>
</ul>